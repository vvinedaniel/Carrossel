{
  "name": "carrossel-instagram-ou-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Carrossel Instagram (8 páginas, 1080x1350) — Instagram ou site? Seu perfil apresenta. O site explica.",
  "scripts": {
    "build": "node src/build.mjs",
    "render": "node scripts/render.mjs",
    "all": "node src/build.mjs && node scripts/render.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
